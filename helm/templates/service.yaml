apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-web.name" . }}
  labels:
    {{- include "business-web.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
  selector:
    app.kubernetes.io/name: {{ include "business-web.name" . }}
